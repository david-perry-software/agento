{
  "name": "agento",
  "version": "0.3.0",
  "private": true,
  "description": "Plan → build → review → ship delivery system for GitHub Copilot, packaged as an agent plugin.",
  "type": "module",
  "scripts": {
    "test": "node --test 'scripts/**/*.test.mjs' 'tests/**/*.test.mjs'",
    "lint:hooks": "shellcheck scripts/hooks/*.sh scripts/wait-for-checks.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
